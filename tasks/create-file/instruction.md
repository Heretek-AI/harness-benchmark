# Task: Create a Greeting File

Create a file called `greeting.txt` containing exactly the text `Hello, World!`.

## Requirements

- The file must be created in the current working directory
- The file must contain exactly `Hello, World!` (no trailing newline required, but acceptable)
- No other files should be created
